[project]
name = "q-quotes-fetcher"
version = "0.2.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
get-passages = "qquotes.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/qquotes"]